# Storage JSON Error Context

## Goal

Expose the storage file path and JSON parser detail when persisted state cannot be parsed.

## Scope

- Preserve fallback handling for missing files.
- Preserve valid JSON read behavior.
- Add focused storage I/O coverage.
